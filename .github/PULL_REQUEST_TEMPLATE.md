## Summary

<!-- What changed and why -->

## Changes

- 

## Testing

- [ ] Added/updated unit tests in `tests/`
- [ ] Ran full suite: `docker compose exec app python -m pytest tests/ -v`
- [ ] Smoke-checked the change end-to-end against the running container
- [ ] Rebuilt with `docker compose up -d --build`

## Bot / Trading Impact

<!-- If this touches a bot, validator, risk gate, or scan loop:
     - Which bot(s)? Crypto / Stock / ThunderBot / Watchdog
     - Paper mode preserved as default? (live trading is opt-in via cb_mode='live')
     - Risk gates still enforced? ($500 daily loss, max positions, kill switch)
     - Scan loop exception handling intact?
     Otherwise delete this section. -->

## Notes

<!-- Anything reviewers should know: rollback plan, follow-ups, etc. -->
